-- Playbook PostgreSQL Schema
-- This file is for reference. Use EF Core migrations for deployment:
--   dotnet ef database update --project backend/Playbook.Api --startup-project backend/Playbook.Api

-- Tables are created by the InitialCreate migration:
-- Teams, Players, Games, Videos, Events, Clips

-- Quick start:
-- 1. Create database: CREATE DATABASE playbook;
-- 2. Update appsettings.json connection string
-- 3. Run: dotnet run (from backend/Playbook.Api) - migrations run automatically
